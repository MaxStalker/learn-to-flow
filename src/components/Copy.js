import styled from "styled-components";

export const SectionTitle = styled.h2`
  color: #111;
  font-size: 2em;
  font-weight: bold;
  margin: 0 0 0.25em;
`;

export const Copy = styled.p`
  color: #333;
  font-size: 1em;
  font-weight: normal;
  margin-bottom: 1em;
  &:last-of-type {
    margin-bottom: 0;
  }
`;
